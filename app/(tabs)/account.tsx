import { ScrollView, Text } from "react-native";

export default function Account()
{
    return (
        <ScrollView>
            <Text>When you log in, your account details will appear here.</Text>
        </ScrollView>
    );
}