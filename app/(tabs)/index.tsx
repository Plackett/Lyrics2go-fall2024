import { ScrollView, Text } from "react-native";

export default function Index() {
    return (
      <ScrollView>
        <Text>
            Welcome to Lyrics2go!
        </Text>
      </ScrollView>  
    );
}