import { ThemedText } from "@/components/ThemedText";
import { ScrollView } from "react-native";

export default function ChrisScreen() {
    return (
        <ScrollView>
            <ThemedText> </ThemedText>
            <ThemedText>  best friends together forever!</ThemedText>
        </ScrollView>
    );
}